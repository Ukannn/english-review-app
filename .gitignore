# Generated browser/test artifacts; keep locally for evidence, but do not treat as source.
.playwright-cli/
output/playwright/
output/
dist/

# Local machine artifacts and dependencies.
.DS_Store
node_modules/

# Local secrets and authentication state. Apps Script project IDs in retained
# rollback snapshots are versioned, but OAuth credentials must never be added.
.env
.env.*
.clasp.json
.clasprc.json
*credentials*.json
*service-account*.json
*.pem
*.key
