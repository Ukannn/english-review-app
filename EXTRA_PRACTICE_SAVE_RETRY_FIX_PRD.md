# 加练题首次保存与自动重试修复 PRD

版本：v0.9.3 补丁

日期：2026-08-12（Asia/Shanghai）

公开代码版本：v0.9.3（生产部署编号不进入公开仓库）

## 1. 问题

v0.9.2 已为正式题的 autosave、锁定和纠正增加单页面写队列、短时锁等待与 `BUSY_RETRY` 自动重试，但题组外的错误强化和完整句挑战仍直接调用 `submitExtraPracticeV4`。该接口最长等待 Script Lock 30 秒，也没有进入前端写队列。锁竞争或响应中断时，第一次点击会弹出保存失败通知；第二次点击通常在锁释放后成功，或通过既有幂等检查读回第一次已经写入的答案。

## 2. 目标

1. 错误强化与完整句挑战第一次点击即可在可恢复的锁竞争下自动完成保存；
2. 加练提交与同页面其他答案写入严格串行；
3. 锁忙时快速返回机器可识别状态，不让用户等待 30 秒后再手动点击；
4. 保留既有加练幂等、精确回读、参考答案延迟披露和 `Affects SRS?=no` 合同。

## 3. 设计

- `submitExtraPracticeV4` 加入现有页面级 Promise 写队列。
- 后端将 `waitLock(30000)` 改为 `tryLock(1000)`；锁忙复用 `BUSY_RETRY`、`retryAfterMs` 响应。
- 前端复用最多三次的有限退避重试。重试携带相同 Session ID、Practice ID 和答案。
- 如果第一次请求已写入但成功响应丢失，自动重试依靠既有 Attempt ID + 相同答案检查返回幂等成功，不新增重复 Review Log 行。
- 非锁忙错误继续直接显示，不自动掩盖身份不匹配、答案覆盖或精确回读失败。

## 4. 非目标与数据边界

- 不修改 Review Log 表头或既有记录；
- 不修改 reinforcement 判分、完整句完成规则或参考答案；
- 不让加练影响 Error Log、Phrase Bank、正式 Result 或 SRS；
- 不允许用不同答案覆盖已经提交的同一加练 Attempt ID。

## 5. 验收标准

1. 模拟前两次 `BUSY_RETRY` 时，单次点击自动重试并成功显示参考答案；
2. 同时提交两道加练时，浏览器最大云端写并发为 1；
3. 自动重试使用同一 Practice ID 和答案；既有幂等路径只产生一条 Review Log；
4. 重试耗尽时恢复按钮并保留输入，用户可以手动重试；
5. 错误强化与完整句挑战均使用相同可靠性路径；
6. 本地语法、合同测试、桌面和 375×812 demo 通过；
7. 发布后 fresh pull 完整文件集与 staging 逐字节一致；具体生产部署编号只记录在私有运维材料中。

## 6. 回退

将现有 production deployment 指回发布前已验证的版本。本补丁没有表结构或数据迁移，不需要删除 Review Log、加练计划或正式学习记录。
