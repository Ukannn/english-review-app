# 连续答题保存性能与超时修复 PRD

版本：v0.9.2 补丁

日期：2026-08-12（Asia/Shanghai）

公开代码版本：v0.9.2（生产部署编号不进入公开仓库）

## 1. 背景与实证

Apps Script version 29 已将 `revealed` 与 `locked` 解耦：标准答案和“下一题”可以立即显示，用户无需等待前一道题云端锁定完成即可继续答题。但保存流水线仍保留以下结构：

1. 输入停止 700 ms 即调用 `saveDraftV4`；
2. 点击锁定时通常先等待/调用 `saveDraftV4`，再调用 `revealAnswerV4`；
3. 不同题目的浏览器请求可以并发进入 Apps Script；
4. `saveDraftV4`、`revealAnswerV4`、`replaceLockedDraftV4` 都等待同一个 Script Lock，最长 15 秒；
5. 每次草稿写入与历史写入分别 `flush` 和回读。

生产数据的只读检查曾观察到：少量连续完成的题目产生了明显更多的云端草稿事件，且相邻题目的事件发生交错。这符合前一道题仍持有全局锁、后一题请求排队并可能超时的现象；具体账户数据与部署证据只保存在私有运维材料中。

## 2. 目标

1. 正常点击“锁定并查看标准答案”只发起一次原子云端锁定请求；
2. 同一个页面的草稿、锁定和纠正请求全局串行，不再相互争抢 Script Lock；
3. 降低半成品自动保存频率，同时继续逐键更新 Session 级浏览器备份；
4. Script Lock 忙时快速返回可恢复状态，由前端有限次数自动重试，不让用户无提示等待 15 秒；
5. 草稿主表和追加式历史在同一次 `flush` 后分别精确回读；
6. 保留 revision 冲突、浏览器备份、Answer Hash、追加式历史和最终提交门禁。

## 3. 非目标

- 不修改任何 Sheet 表头或数据合同；
- 不放宽跨页面 revision 冲突判断；
- 不允许未完成云端锁定的题进入最终提交；
- 不修改出题、批改、SRS、加练、语料箱或题数逻辑；
- 不把等待时间从 15 秒简单延长为更大的值；
- 不删除既有草稿或历史记录。

## 4. 设计

### 4.1 浏览器本地备份与自动保存

- 每次输入仍同步更新内存与 `sessionStorage`；页面崩溃或刷新时可以恢复完整文本。
- 云端 autosave 防抖从 700 ms 调整为 2000 ms。
- 切题、页面隐藏或离开今日学习区时仍触发一次有序保存。
- 已进入 `revealed/syncing` 的题不再因为导航重复发起 autosave；由该题的锁定 Promise 负责完成云端状态。

### 4.2 单页面全局写队列

- `saveDraftV4`、`revealAnswerV4`、`replaceLockedDraftV4` 共用一个 Promise 队列。
- 队列按用户动作进入顺序逐个执行；一个请求无论成功或失败，都不得永久阻断后续请求。
- 不同题目的 UI 与输入不被队列阻塞；队列只限制云端写并发。

### 4.3 单次原子锁定

- 正常路径中，如果本题没有已经在途的旧 autosave，点击锁定直接调用 `revealAnswerV4`。
- `revealAnswerV4` 在一个 Script Lock 内完成 revision 校验、答案写入、Answer Hash、历史追加、flush 和双表回读。
- 如果点击前已有同题 autosave 在途，则只等待该已发生请求结束，再用最新 revision 调用一次锁定；不主动新增前置 autosave。

### 4.4 锁忙与自动重试

- 三个题级写接口最多尝试获取 Script Lock 1000 ms。
- 未获得锁时返回 `code=BUSY_RETRY`、`busy=true` 和建议重试延迟，不抛出 15 秒锁超时。
- 前端在同一写队列中以有限退避自动重试；每次重试继续携带原 expected revision。
- 重试耗尽后保留本地答案与标准答案，标记本题尚未同步并提供手动重试。

## 5. 状态与安全合同

| 状态 | 可继续答题 | 可最终提交 | 恢复方式 |
| --- | --- | --- | --- |
| 本地已输入、等待 autosave | 是 | 否 | Session 备份 + 队列保存 |
| 已显示答案、等待锁定 | 是 | 否 | 自动重试或手动重试 |
| 云端忙、正在退避重试 | 是 | 否 | 有限自动重试 |
| revision 冲突 | 是 | 否 | 显式选择本地/云端/暂不处理 |
| `locked=true` 且 hash 回读一致 | 是 | 是 | 正常提交 |

任何自动重试都不得绕过 expected revision；冲突必须继续进入既有人工选择界面。

## 6. 验收标准

1. 正常输入后在 2 秒内点击锁定：只调用一次 `revealAnswerV4`，不调用 `saveDraftV4`。
2. 连续锁定两题：第二题云端写在第一题结束后才开始，但第二题 UI 可以立即输入和显示答案。
3. 2 秒 autosave 已经开始时继续补全并锁定：最终锁定完整答案，不产生错误覆盖或假冲突。
4. 模拟前两次 `BUSY_RETRY`：自动退避重试后成功，用户不看到超时错误。
5. 模拟重试耗尽：标准答案与浏览器备份保留，本题不可提交且可手动重试。
6. 正常单题产生最多一个 `reveal_lock`；只有真实停顿、导航或在途 autosave 才额外产生 `autosave`。
7. 草稿行与历史行在同一次 flush 后均精确回读；任一回读不匹配则本次请求失败。
8. Apps Script、HTML 内联 JavaScript、合同测试、桌面与 375×812 demo 全部通过。
9. 生产部署完成后执行 fresh pull，并确认完整源码集合与 staging 逐字节一致；具体部署编号仅记录在私有运维材料中。

## 7. 发布、监测与回退

- 项目版本：v0.9.2；Apps Script 部署编号与生产部署地址不进入公开仓库。
- 私有和公开仓库均执行 branch → commit → push → PR → merge → tag → GitHub Release。
- 公开仓库仅包含脱敏源码、通用 PRD/README 和测试，不包含 Spreadsheet ID、Deployment ID、私有链接、`.clasp.json`、`HANDOFF.md` 或 `rollback/`。
- 发布后以 `?demo=1` 验证单请求锁定、连续两题串行、忙重试、失败门禁和移动端布局；不写正式学习数据。
- 回退时将 production deployment 指回发布前已验证的版本；本补丁无表结构迁移，不需要删除或回滚学习记录。
